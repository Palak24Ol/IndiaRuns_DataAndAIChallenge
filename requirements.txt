# Redrob AI Ranker — Dependencies
# The ranking pipeline uses Python standard library only.
# No external packages are required for ranking.

# Optional: for Streamlit demo (sandbox/demo link)
# streamlit>=1.30.0

# Optional: for submission presentation generation
# python-pptx>=1.0.0

# Optional: for reading .docx documentation files
# python-docx>=1.0.0
